name: PR Checks Passed
on:
  workflow_run:
    workflows:
      - Server CI
      - Check Documentation
    types:
      - completed

jobs:
  check:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion != 'success' }}
    steps:
      - name: Workflow Failed
        run: |
          echo "Either the C/C++ CI or Check Documentation workflow must pass."
          exit 1
